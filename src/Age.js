import React from 'react';

const Age = ({ age }) => {
    return (
        <div style={{padding:'20px', border: '1px solid blue'}}>
            <h1>My Age is {age}</h1>
        </div>
    )
}

export default Age;